The data has been truncated to keep the size of this repo down. The `test.csv`
file was originally 70MB. But that's not hard to restore, and we don't need the
exact original `test.csv`. Just search the internet for "sudoku dataset", and
pick any in a format that is the same as the example `test.csv` here.

Enjoy.
